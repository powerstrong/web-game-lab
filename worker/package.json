{
  "name": "game-lobby-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml --persist-to \"%TEMP%/web-game-lab-wrangler-state\"",
    "deploy": "wrangler deploy --config wrangler.toml",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
